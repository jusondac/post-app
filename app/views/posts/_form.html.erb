<div class="min-h-screen bg-gradient-to-br from-blue-50 via-white to-purple-50">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <!-- Header Section -->
    <div class="text-center mb-12">
      <div class="inline-flex items-center justify-center w-16 h-16 bg-gradient-to-r from-blue-600 to-purple-600 rounded-full mb-6">
        <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <% if local_assigns[:edit_mode] %>
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
          <% else %>
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
          <% end %>
        </svg>
      </div>
      <h1 class="text-4xl font-bold text-gray-900 mb-4">
        <%= local_assigns[:edit_mode] ? "Edit Your Post" : "Create New Post" %>
      </h1>
      <p class="text-xl text-gray-600 max-w-2xl mx-auto">
        <%= local_assigns[:edit_mode] ? "Update your post and share your thoughts with the world" : "Share your thoughts and ideas with the world" %>
      </p>
    </div>
    <!-- Form Section -->
    <div class="bg-white rounded-2xl shadow-2xl overflow-hidden">
      <div class="px-8 py-10">
        <%= form_with model: @post, local: true, class: "space-y-8" do |form| %>
          <!-- Error Messages -->
          <% if @post.errors.any? %>
            <div class="bg-red-50 border-l-4 border-red-400 rounded-lg p-6">
              <div class="flex">
                <div class="flex-shrink-0">
                  <svg class="h-6 w-6 text-red-400" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
                  </svg>
                </div>
                <div class="ml-3">
                  <h3 class="text-lg font-semibold text-red-800 mb-2">Please fix the following errors:</h3>
                  <ul class="text-sm text-red-700 space-y-1">
                    <% @post.errors.full_messages.each do |message| %>
                      <li class="flex items-center">
                        <span class="w-1.5 h-1.5 bg-red-400 rounded-full mr-2"></span>
                        <%= message %>
                      </li>
                    <% end %>
                  </ul>
                </div>
              </div>
            </div>
          <% end %>
          <!-- Title Field -->
          <div class="space-y-2">
            <%= form.label :title, class: "flex items-center text-sm font-semibold text-gray-700" do %>
              <svg class="w-4 h-4 mr-2 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"></path>
              </svg>
              Post Title
            <% end %>
            <%= form.text_field :title, 
                class: "w-full px-4 py-4 text-lg border-2 border-gray-200 rounded-xl focus:ring-4 focus:ring-blue-100 focus:border-blue-500 transition duration-300 bg-gray-50 hover:bg-white",
                placeholder: "Enter an engaging title for your post..." %>
            <p class="text-sm text-gray-500">Choose a clear, descriptive title that captures your post's essence</p>
          </div>
          <!-- Content Field -->
          <div class="space-y-2">
            <%= form.label :content, class: "flex items-center text-sm font-semibold text-gray-700" do %>
              <svg class="w-4 h-4 mr-2 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
              </svg>
              Content
            <% end %>
            <%= form.text_area :content, 
                rows: 15,
                class: "w-full px-4 py-4 text-lg border-2 border-gray-200 rounded-xl focus:ring-4 focus:ring-blue-100 focus:border-blue-500 transition duration-300 bg-gray-50 hover:bg-white resize-vertical leading-relaxed",
                placeholder: "Write your post content here... Share your thoughts, ideas, and insights with the world." %>
            <p class="text-sm text-gray-500">Write engaging content that provides value to your readers</p>
          </div>
          <!-- Publisher and Status Row -->
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <!-- Publisher Field -->
            <div class="space-y-2">
              <%= form.label :publisher_id, class: "flex items-center text-sm font-semibold text-gray-700" do %>
                <svg class="w-4 h-4 mr-2 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
                </svg>
                Publisher
              <% end %>
              <%= form.collection_select :publisher_id, @publishers, :id, :name, 
                  { prompt: "🏢 Select a publisher (optional)" },
                  { class: "w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:ring-4 focus:ring-blue-100 focus:border-blue-500 transition duration-300 bg-gray-50 hover:bg-white" } %>
              <p class="text-sm text-gray-500">Choose where to publish your post</p>
            </div>
            <!-- Status Field -->
            <div class="space-y-2">
              <%= form.label :status, class: "flex items-center text-sm font-semibold text-gray-700" do %>
                <svg class="w-4 h-4 mr-2 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
                Status
              <% end %>
              <%= form.select :status, 
                  options_for_select([
                    ['📝 Draft - Save for later', 'draft'], 
                    ['🚀 Published - Share with world', 'published']
                  ], @post.status || 'draft'),
                  {},
                  { class: "w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:ring-4 focus:ring-blue-100 focus:border-blue-500 transition duration-300 bg-gray-50 hover:bg-white" } %>
              <p class="text-sm text-gray-500">
                <% if local_assigns[:edit_mode] %>
                  Update the visibility of your post
                <% else %>
                  Choose whether to save as draft or publish immediately
                <% end %>
              </p>
            </div>
          </div>
          <!-- Action Buttons -->
          <div class="flex flex-col sm:flex-row justify-between items-center pt-8 space-y-4 sm:space-y-0">
            <div class="flex space-x-4">
              <%= link_to local_assigns[:edit_mode] ? post_path(@post) : posts_path, 
                  class: "inline-flex items-center px-6 py-3 border-2 border-gray-300 rounded-xl text-gray-700 bg-white hover:bg-gray-50 hover:border-gray-400 font-medium transition duration-300 group" do %>
                <svg class="w-5 h-5 mr-2 group-hover:-translate-x-1 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                </svg>
                Cancel
              <% end %>
              <% if local_assigns[:edit_mode] %>
                <%= link_to "Preview", post_path(@post), 
                    class: "inline-flex items-center px-6 py-3 border-2 border-blue-300 rounded-xl text-blue-700 bg-blue-50 hover:bg-blue-100 hover:border-blue-400 font-medium transition duration-300",
                    target: "_blank" %>
              <% end %>
            </div>
            <div class="flex space-x-3">
              <% unless local_assigns[:edit_mode] %>
                <%= form.submit "💾 Save Draft", 
                    name: "draft",
                    class: "inline-flex items-center px-6 py-3 border-2 border-gray-400 rounded-xl text-gray-700 bg-gray-100 hover:bg-gray-200 font-medium transition duration-300 cursor-pointer" %>
              <% end %>
              <%= form.submit local_assigns[:edit_mode] ? "🚀 Update Post" : "🚀 Publish Post", 
                  class: "inline-flex items-center px-8 py-3 bg-gradient-to-r from-blue-600 to-purple-600 text-white font-semibold rounded-xl hover:from-blue-700 hover:to-purple-700 transition duration-300 transform hover:scale-105 shadow-lg cursor-pointer" %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
    <!-- Tips Section -->
    <div class="mt-12 bg-gradient-to-r from-blue-50 to-purple-50 rounded-2xl p-8">
      <h3 class="text-xl font-semibold text-gray-800 mb-4 flex items-center">
        <svg class="w-6 h-6 mr-2 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
        </svg>
        💡 Writing Tips
      </h3>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 text-sm text-gray-600">
        <div class="flex items-start">
          <span class="flex-shrink-0 w-6 h-6 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center text-xs font-bold mr-3 mt-0.5">1</span>
          <div>
            <strong class="text-gray-700">Engaging Title:</strong> Use clear, descriptive titles that grab attention and accurately represent your content.
          </div>
        </div>
        <div class="flex items-start">
          <span class="flex-shrink-0 w-6 h-6 bg-purple-100 text-purple-600 rounded-full flex items-center justify-center text-xs font-bold mr-3 mt-0.5">2</span>
          <div>
            <strong class="text-gray-700">Quality Content:</strong> Write valuable, well-structured content that provides insights or solutions to your readers.
          </div>
        </div>
        <div class="flex items-start">
          <span class="flex-shrink-0 w-6 h-6 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-xs font-bold mr-3 mt-0.5">3</span>
          <div>
            <strong class="text-gray-700">Save Drafts:</strong> Use drafts to refine your content before publishing. You can always come back and edit later.
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
